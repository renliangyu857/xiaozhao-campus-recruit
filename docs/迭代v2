# 迭代 v2 - 纪要与变更

**基准 commit**：`8af650a` feat: 迭代v1 留存与转化 + 登录自动加载 + 邀请有礼  
**完成内容**：邀请奖励规则调整、会员价位可配置、权益对比看板、邀请/会员页滚动、3 个月超值推荐、列表收藏与「我的收藏」筛选、今日新增按钮修复等。

---

## 一、数据库表结构

**本次迭代无需变更数据库表结构。**

- 现有表 `app_user`、`user_invitation`、`user_member`、`payment_order`、`job`、`user_job_status`、`referral_code` 等均未新增或修改字段。
- 邀请奖励规则调整（前 5 人 2 天/人，第 6 人起 4 天/人，最高 30 天）仅修改 `InviteService` 内逻辑，不涉及表结构。
- 会员价位改为配置下发（`app.vip.plans`），未新增表。
- 职位收藏当前为前端 localStorage（`campusrecruit_favorites_${userId}`），未使用服务端表。

### 可选：后续若将收藏持久化到服务端

若后续需要跨设备同步收藏，可新增表并增加接口，例如：

```sql
-- 可选：职位收藏表（仅当需要服务端持久化时执行）
CREATE TABLE IF NOT EXISTS job_favorite (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id BIGINT NOT NULL,
  job_id VARCHAR(64) NOT NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_user_job (user_id, job_id),
  KEY idx_user_id (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户收藏的职位';
```

当前版本**不需要**执行上述 SQL。

---

## 二、自上次 commit 起的修改清单

### 后端 (backend)

| 路径 | 变更说明 |
|------|----------|
| `src/main/java/com/campusrecruit/service/InviteService.java` | 邀请奖励改为前 5 人每人 2 天 VIP、第 6–10 人每人 4 天、上限 30 天；被邀请人仍 2 天；stats 文案同步 |
| `src/main/java/com/campusrecruit/service/VipPlanService.java` | 套餐改为从 VipPlanProperties 配置合并默认值；3 个月默认价 16.6、标签「超值推荐」 |
| `src/main/java/com/campusrecruit/config/VipPlanProperties.java` | **新增**：`@ConfigurationProperties(prefix="app.vip")`，支持 app.vip.plans 下发价位/标签 |
| `src/main/resources/application.yml` | 增加 app.vip.plans 注释说明，便于运维调整价位 |
| `src/test/.../VipControllerTest.java` | 断言 3 个月价格为 16.6、tag 为「超值推荐」 |

### 前端 (frontend)

| 路径 | 变更说明 |
|------|----------|
| `pages/InvitePage.tsx` | 副标题与进度节点改为 0/5/10、前 5 人 2 天/之后 4 天/最高 1 个月；顶部滚动条 30 条「mpweixin*** 已邀请 N 人获得 R」打乱展示；移除微信好友/朋友圈按钮 |
| `pages/VIPPage.tsx` | 会员选择改为权益对比看板（表格）；3 个月列默认推荐（橙色边框+超值推荐角标+琥珀按钮）；滚动条改为「用户*** 已购买 X 会员」「已投递 N 家职位抢占先机」（已去掉「已有 N 用户购买 VIP」） |
| `pages/HomePage.tsx` | 「今日新增」点击时带当前筛选重新 loadPage；新增「我的收藏」按钮与 showFavoritesOnly；收藏列表过滤、标题/条数联动；集成 favoriteService 与 JobCard 收藏回调 |
| `components/JobCard.tsx` | 新增收藏星标按钮与 isFavorite/onToggleFavorite props |
| `services/favoriteService.ts` | **新增**：getFavoriteJobIds、toggleFavoriteJobId，localStorage 按用户存 job id |
| `constants.ts` | VIP_PLANS 兜底改为 3 个月 16.6、「超值推荐」；注释说明以接口为准 |

### 文档与规划

| 路径 | 变更说明 |
|------|----------|
| `docs/迭代v1-留存与转化-纪要.md` | （若有小修） |
| `task_plan.md` | 权益对比看板、滚动展示、邀请奖励、会员价位、收藏功能等阶段记录 |
| `findings.md` | 权益与套餐、滚动规则、邀请规则、会员价位、收藏实现等结论 |

---

## 三、变更的 SQL 语句（本次必须执行）

**无。** 本次迭代不涉及表结构变更，无需执行任何 SQL。

（可选 SQL 见第一节「可选：后续若将收藏持久化到服务端」。）
