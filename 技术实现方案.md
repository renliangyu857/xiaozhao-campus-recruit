# 校招信息聚合网站 - 技术实现方案

## 一、项目概述

校招毕业生招聘信息聚合查询网站：主站展示招聘列表，支持筛选、投递进度管理、会员与微信支付。前端已完成（React + Vite），本方案约定后端技术选型、数据模型、接口与各功能实现方式，供确认后按敏捷节奏逐功能开发。

---

## 二、技术栈与架构

### 2.1 后端技术栈

| 技术 | 说明 |
|------|------|
| Java 17+ | JDK 版本 |
| Spring Boot 3.x | Web、安全、数据 |
| Spring Data JPA | ORM，可选配合 QueryDSL 做复杂查询 |
| MySQL 8.x | 主库，存储业务数据 |
| Redis | 会话存储（多节点会话共享）、缓存、限流 |
| Spring Session Data Redis | 会话持久化到 Redis |
| 微信开放平台 / 公众号 | 网页授权登录、JSAPI 支付 |

### 2.2 部署与会话

- **多节点部署**：无状态应用，会话全部存 Redis（Spring Session + Redis）。
- **会话共享**：同一用户在不同节点命中同一 Redis Session，实现会话共享。

---

## 三、数据库数据模型设计

### 3.1 表结构说明

#### 3.1.1 招聘信息表 `job`

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT PK AUTO_INCREMENT | 主键 |
| company | VARCHAR(128) | 公司名称 |
| industry | VARCHAR(32) | 公司行业：互联网、金融、国央企、外企、制造业、其他 |
| recruit_type | VARCHAR(32) | 招聘类型：秋招、春招、实习 |
| locations | VARCHAR(512) | 工作地点，JSON 数组字符串，如 ["北京","上海"] |
| start_date | DATE | 开始时间 |
| end_date | DATE | 结束时间 |
| no_written_test | TINYINT(1) | 是否免笔试：0否 1是 |
| roles | VARCHAR(512) | 岗位，JSON 数组字符串，如 ["前端","后端"] |
| announcement_link | VARCHAR(512) | 招聘公告链接 |
| apply_link | VARCHAR(512) | 投递链接 |
| remark | VARCHAR(1024) | 备注 |
| batch | VARCHAR(8) | 届次：25、26 |
| salary | VARCHAR(64) | 薪资描述 |
| created_at | DATETIME | 创建时间（用于“今日新增”判断） |
| updated_at | DATETIME | 更新时间 |

- **索引建议**：`industry`、`recruit_type`、`end_date`、`created_at`，便于筛选与“今日新增”。

#### 3.1.2 用户表 `app_user`（表名避免保留字）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT PK AUTO_INCREMENT | 主键 |
| open_id | VARCHAR(64) UNIQUE | 微信公众号 openid，唯一 |
| union_id | VARCHAR(64) | 开放平台 unionid（可选） |
| nickname | VARCHAR(64) | 昵称 |
| avatar | VARCHAR(512) | 头像 URL |
| query_count | INT DEFAULT 0 | 免费查询次数（每日或重置策略见下） |
| query_count_reset_at | DATE | 上次重置免费次数的日期（按日重置时用） |
| created_at | DATETIME | 注册时间 |
| updated_at | DATETIME | 更新时间 |

#### 3.1.3 会员表 `user_member`（采用方案 A：独立会员表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT PK AUTO_INCREMENT | 主键 |
| user_id | BIGINT FK | 用户 ID |
| plan_id | VARCHAR(32) | 套餐：1_month / 3_month / lifetime |
| start_at | DATETIME | 生效时间 |
| end_at | DATETIME | 截止时间（永久可为 2099-12-31 或 NULL） |
| created_at | DATETIME | 开通时间 |

- 以当前时间在 `[start_at, end_at]` 内且 `user_id` 匹配的最新一条记录判断是否 VIP 及截止日期。

#### 3.1.4 用户投递进度表 `user_job_status`

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT PK AUTO_INCREMENT | 主键 |
| user_id | BIGINT FK | 用户 ID |
| job_id | BIGINT FK | 职位 ID |
| status | VARCHAR(32) | 未投递、已投递、已笔试、已面试、已通过、已挂 |
| updated_at | DATETIME | 更新时间 |

- **唯一约束**：`(user_id, job_id)`，一个用户对同一职位一条记录，更新 status 即可。

#### 3.1.5 会员套餐表 `vip_plan`（可选，也可写死在代码）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | VARCHAR(32) PK | 1_month / 3_month / lifetime |
| name | VARCHAR(32) | 显示名 |
| duration_days | INT | 有效天数，永久可用 99999 或 NULL |
| price | DECIMAL(10,2) | 价格（元） |
| sort_order | INT | 排序 |

#### 3.1.6 支付订单表 `payment_order`

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT PK AUTO_INCREMENT | 主键 |
| order_no | VARCHAR(64) UNIQUE | 业务订单号 |
| user_id | BIGINT FK | 用户 ID |
| plan_id | VARCHAR(32) | 套餐 ID |
| amount | DECIMAL(10,2) | 金额（元） |
| pay_channel | VARCHAR(16) | 如 wechat_jsapi |
| trade_no | VARCHAR(64) | 微信支付单号（支付成功后回填） |
| status | VARCHAR(16) | pending / success / failed / closed |
| paid_at | DATETIME | 支付成功时间 |
| created_at | DATETIME | 创建时间 |

---

## 四、接口列表

以下为 REST 风格，基础路径示例：`/api`。除登录、微信回调外，需登录态（Session 或 Token）。

### 4.1 认证与用户

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/auth/wechat/login | 微信网页授权回调后，用 code 换 openid 并登录，写 Session，返回当前用户信息 |
| GET | /api/auth/current | 获取当前登录用户（含 VIP 状态、剩余免费次数、会员截止日） |
| POST | /api/auth/logout | 登出（清除服务端 Session） |

### 4.2 招聘列表与筛选

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/jobs | 分页列表，Query：industry, type, location, deadlineDays(3/7/15/ALL), roles(逗号分隔), onlyNewToday(true/false), page, size；返回带当前用户每条 job 的 status（来自 user_job_status） |
| GET | /api/jobs/{id} | 职位详情（可选） |

- **说明**：  
  - 若未登录，可只返回列表，不返回个人 status 或全部为“未投递”。  
  - “今日新增”：`created_at >= 当日 00:00:00` 的筛选由后端做。

### 4.3 投递进度

| 方法 | 路径 | 说明 |
|------|------|------|
| PUT | /api/jobs/{jobId}/status | Body: { "status": "已投递" }，更新当前用户对该 job 的投递进度 |

### 4.4 查询次数与会员

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/query/consume | 执行一次“查询”扣减（仅非 VIP 且免费次数未用完时扣减），返回剩余次数或 0；若已超 3 次返回 403 或业务码，前端跳会员页 |
| GET | /api/vip/plans | 会员套餐列表（1 个月 8.8 元、3 个月 18.8 元、永久 29.9 元） |
| POST | /api/vip/create-order | Body: { "planId": "1_month" }，创建支付订单，返回微信 JSAPI 调起支付所需参数（或支付 URL） |
| GET | /api/vip/orders | 当前用户订单列表（可选，用于“我的订单”） |

### 4.5 进度统计（进度管理页）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/progress/stats | 返回各状态数量及按行业统计等，供前端饼图、柱状图使用 |

### 4.6 微信支付回调（服务端）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/pay/wechat/notify | 微信支付异步通知，验签、更新订单、开通/延长会员，返回成功 XML |

---

## 五、各功能技术方案

### 5.1 列表展示与筛选

- **列表**：`GET /api/jobs`，分页（page/size），默认按 `created_at` 或 `end_date` 排序。
- **筛选**：  
  - industry、recruit_type、location：精确或模糊（location 用 LIKE 或 JSON_CONTAINS 视 locations 存储方式而定）。  
  - deadlineDays：仅保留 `end_date` 在今日起 3/7/15 天内的记录。  
  - roles：前端传逗号分隔，后端拆分为多个关键词，对 `roles` 做 OR 匹配（JSON 数组或 LIKE）。  
  - onlyNewToday：`created_at >= 当天 00:00:00`。
- **用户维度进度**：列表查询时 left join `user_job_status`（按当前 user_id），每条 job 带上 status；未登录则不带或默认“未投递”。
- **“今日新增”**：同上，由后端按 `created_at` 过滤，前端仅传 `onlyNewToday=true`。

### 5.2 微信公众号登录

- **流程**：  
  1. 前端跳转微信授权 URL（appId、redirect_uri、scope=snsapi_userinfo 或 snsapi_base）。  
  2. 用户同意后微信带上 code 回调 redirect_uri。  
  3. 后端用 code 换 access_token 和 openid（及昵称头像若 scope 为 userinfo）。  
  4. 根据 openid 查/建 user，写入 Session（Spring Session Redis），并生成或返回前端需要的用户信息（如 id、nickname、avatar、isVip、queryCount、vipExpiry）。  
- **实现**：Spring 里一个 Controller 处理回调 URL，调用微信 HTTP 接口，再重定向到前端首页或指定页并携带 Session Cookie。  
- **公众号二维码**：后续由你提供，前端登录页放二维码与“微信内打开链接授权”的入口即可。

### 5.3 会话共享（多节点）

- 使用 **Spring Session + Redis**：  
  - 依赖：`spring-session-data-redis`，Session 存储改为 Redis。  
  - 所有节点连接同一 Redis，Session 以 Cookie 中 session id 为 key 存在 Redis，即可跨节点共享。  
- 无需 sticky session；负载均衡可任意轮询。

### 5.4 投递进度保存

- 前端在列表/详情中点选状态后调用 `PUT /api/jobs/{jobId}/status`，Body 传 `{ "status": "已投递" }` 等。  
- 后端根据 Session 取 user_id，对 `user_job_status` 做 upsert（有则 update，无则 insert），保证 (user_id, job_id) 唯一。  
- 进度管理页的统计接口基于 `user_job_status` 聚合即可。

### 5.5 免费 3 次查询与会员

- **规则**：未开通 VIP 时，每执行一次“查询筛选”计 1 次；当日（或按你定的重置策略）最多 3 次，超过后前端提示开通会员并跳转会员页。  
- **实现**：  
  - 方案 1：前端在点击“查询筛选”时先调 `POST /api/query/consume`，成功则再调 `GET /api/jobs`；若 403 或返回“次数用尽”，则弹窗并跳转会员页。  
  - 方案 2：后端在 `GET /api/jobs` 时根据 Query 是否“有效筛选”自动扣减次数（需防重复扣减，可用 Redis 计数 + 每日重置）。  
- **重置**：建议按自然日重置（每日 0 点将 `query_count` 置 0 或按 `query_count_reset_at` 判断）；可在每日首次请求时或定时任务中重置。  
- **VIP**：根据 `user_member` 或 user 表 VIP 字段判断，为 VIP 则不扣减、不限制次数。  
- **会员页**：已开通则只展示会员周期与截止日期（从 `user_member` 或 user 表取），不展示“立即开通”按钮；未开通则展示套餐与“立即开通”。

### 5.6 会员充值与微信支付

- **套餐**：1 个月 8.8 元、3 个月 18.8 元、永久 29.9 元，与前端 `VIP_PLANS` 一致，可写死或存 `vip_plan` 表。  
- **流程**：  
  1. 用户选套餐，前端调 `POST /api/vip/create-order`，传入 planId。  
  2. 后端生成订单（payment_order），调用微信统一下单（JSAPI），返回前端调起支付所需参数。  
  3. 前端在微信内调起微信支付，用户付款。  
  4. 微信异步回调 `POST /api/pay/wechat/notify`，后端验签、更新订单状态、开通/续期会员（写 user_member 或更新 user 表），再返回成功。  
  5. 前端可通过轮询订单状态或微信支付成功回调页跳转会员页，展示最新会员状态。  
- **安全**：验签使用微信提供的签名算法；订单与金额以服务端为准，不可信任前端传参。

### 5.7 进度管理页

- 接口：`GET /api/progress/stats`。  
- 后端从 `user_job_status` 按 status 聚合数量，按 industry 聚合（可与 job 表 join 取 industry）。  
- 返回结构示例：  
  - `{ "byStatus": { "已投递": 10, "已笔试": 3, ... }, "byIndustry": [ { "industry": "互联网", "count": 5 }, ... ], "totalApplied": 18 }`  
- 前端现有饼图、柱状图可直接使用该结构。

---

## 六、安全与稳定性

### 6.1 安全

- **登录与鉴权**：所有需登录接口校验 Session（或 JWT 若后续改为 Token）；微信登录仅信任微信返回的 openid。  
- **防重放与篡改**：支付回调严格验签，订单状态幂等更新（已成功的订单不再改会员）。  
- **接口安全**：  
  - 生产环境 HTTPS。  
  - 敏感接口（如创建订单、更新进度）可加 CSRF、限流（见下）。  
- **数据**：用户 openid、订单号等敏感信息按需脱敏日志；密码不涉及（微信登录无密码）。

### 6.2 稳定性与性能

- **多节点**：无状态 + Redis Session，已支持水平扩展。  
- **限流**：对 `/api/query/consume` 或 `/api/jobs` 按 user_id 限流（如 Redis 计数），防止刷接口。  
- **缓存**：招聘列表可对“无用户维度”的查询结果做短时缓存（如 1 分钟）；带用户进度的列表可不缓存或短缓存。  
- **数据库**：为 industry、recruit_type、end_date、created_at 建索引；分页使用 limit/offset 或 better 游标分页，避免深分页。  
- **微信与支付**：支付回调做幂等；回调逻辑尽量快速返回，复杂逻辑可异步（如发 MQ 再处理开通会员）。

### 6.3 运维与监控

- 健康检查：`/actuator/health`（Spring Boot Actuator）。  
- 日志：请求 ID、user_id、关键操作打日志，便于排查。  
- 监控：按需接入监控与告警（如订单失败、支付回调异常）。

---

## 七、与前端联调约定

- **基础路径**：如 `https://your-api.com/api`，前端请求该基础路径。  
- **跨域**：后端配置 CORS，允许前端域名；若前后端同域则无需额外 CORS。  
- **登录态**：Cookie 方式（Spring Session 默认），前端请求携带 Cookie；若前后端分离且跨域，需配置 Cookie 的 SameSite、Secure 等。  
- **数据格式**：  
  - 列表与前端 `Job` 对齐：industry/type/status 用中文或与前端枚举一致；locations、roles 为数组；日期 ISO 或 `yyyy-MM-dd`。  
  - 用户信息与前端 `User` 对齐：id、nickname、avatar、isVip、vipExpiry、queryCount（及剩余次数）等。  
- **错误码**：如 200 成功，401 未登录，403 免费次数用尽，400 参数错误，500 服务器错误；业务码可用 JSON body 中 code/message 表示。

---

## 八、开发顺序建议（敏捷、逐功能）

1. **基础**：Spring Boot 项目骨架、MySQL/Redis 配置、表结构创建、Spring Session Redis。  
2. **招聘列表与筛选**：job 表 CRUD、GET /api/jobs（含筛选、分页、今日新增），暂不接用户进度。  
3. **用户与登录**：user 表、微信登录接口、Session、GET /api/auth/current、登出。  
4. **投递进度**：user_job_status 表、PUT /api/jobs/{jobId}/status，列表接口返回用户维度 status。  
5. **免费次数与会员逻辑**：query_count 与重置、POST /api/query/consume、会员判断、GET /api/vip/plans。  
6. **支付**：payment_order、**user_member（方案 A）**、创建订单、微信 JSAPI 支付、回调验签与开通会员。  
7. **进度统计**：GET /api/progress/stats。  
8. **联调与测试**：与前端逐页联调（首页、会员页、进度页）、异常与边界测试。

---

## 九、敏捷与测试策略（强制）

- **会员表**：采用 **方案 A**（独立表 `user_member`）。
- **每个功能点完成代码后**：
  1. 为该功能生成 **完整测试用例**（单元测试 + 接口/集成测试，覆盖正常与边界、未登录/无权限等）。
  2. 运行 **自动化测试**，必须全部通过后再进入下一功能。
  3. 若测试不通过：根据测试报错 **自动修复代码**，直至该功能所有用例通过，再继续下一功能。
- 测试框架：JUnit 5 + Spring Boot Test（MockMvc / TestContainers 或 H2 内存库可选），接口层与关键业务层均需覆盖。
