# 校招信息聚合站 - 配置与启动说明

本文档说明前端、后端的运行环境、配置方式及启动步骤。

---

## 一、后端（Spring Boot）

### 1.1 环境要求

| 项目     | 要求                          |
|----------|-------------------------------|
| JDK      | 17 或以上                     |
| Maven    | 3.6+（或使用 IDE 自带）       |
| MySQL    | 8.0+（或 5.7+）               |
| Redis    | 6.0+（Session 存储）         |

### 1.2 数据库准备

1. 安装并启动 MySQL。
2. 创建库（库名与 `application.yml` 中一致，默认 `campusrecruit`）：

```sql
CREATE DATABASE IF NOT EXISTS campusrecruit
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;
```

3. 创建具备该库读写权限的用户（可选，若用 root 可跳过）：

```sql
CREATE USER 'campusrecruit'@'localhost' IDENTIFIED BY '你的密码';
GRANT ALL PRIVILEGES ON campusrecruit.* TO 'campusrecruit'@'localhost';
FLUSH PRIVILEGES;
```

### 1.3 Redis 准备

- 安装并启动 Redis，默认监听 `localhost:6379`。
- Windows 可使用 WSL 或 Redis for Windows 便携版；Linux/macOS 一般 `redis-server` 即可。

### 1.4 配置文件

主配置在 **`backend/src/main/resources/application.yml`**：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campusrecruit?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: ${DB_PASSWORD:}
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080
  servlet:
    context-path: /api
```

常用调整：

- **数据库密码**：在环境变量中设置 `DB_PASSWORD`，或把 `${DB_PASSWORD:}` 改为固定密码（仅限本地开发）。
- **MySQL 地址/库名/用户名**：修改 `spring.datasource` 下 `url`、`username`。
- **Redis 地址/端口**：修改 `spring.data.redis` 下 `host`、`port`。
- **服务端口与上下文**：默认 `http://localhost:8080/api`，改 `server.port` 与 `context-path` 即可。

### 1.5 微信相关（可选）

- **未配置或本地开发**：不设置 `wechat.app-id` 时，登录与支付均为 **Stub 模式**（code 直接当 openid，支付返回 stub 参数）。
- **正式环境**：在 `application.yml` 或 `application-prod.yml` 中增加：

```yaml
wechat:
  app-id: 你的小程序/公众号AppID
  app-secret: 你的AppSecret
  stub: false
wechat:
  pay:
    stub: false
    # 并配置 mch-id、api-v3-key、证书等（按现有支付实现补全）
```

### 1.6 启动方式

在项目根目录下进入后端目录并启动：

```bash
cd backend
mvn spring-boot:run
```

或先打包再运行：

```bash
cd backend
mvn -q package -DskipTests
java -jar target/backend-1.0.0-SNAPSHOT.jar
```

**环境变量示例（按需使用）：**

```bash
# Windows (CMD)
set DB_PASSWORD=你的MySQL密码
mvn spring-boot:run

# Windows (PowerShell)
$env:DB_PASSWORD="你的MySQL密码"; mvn spring-boot:run

# Linux / macOS
export DB_PASSWORD=你的MySQL密码
mvn spring-boot:run
```

启动成功后：

- 接口基址：**http://localhost:8080/api**
- 例如：招聘列表 `GET http://localhost:8080/api/jobs`、当前用户 `GET http://localhost:8080/api/auth/current`。

### 1.7 运行测试

```bash
cd backend
mvn test
```

测试使用 H2 内存库和 `application-test.yml`，无需 MySQL/Redis。

### 1.8 使用 Supabase 作为数据库（可选）

Supabase 提供的是 **PostgreSQL**，不是 MySQL，因此需要改**配置和依赖**，**业务代码和实体无需改**（JPA 兼容）。

**已做的改动：**

1. **依赖**（`backend/pom.xml`）：已增加 `postgresql` 驱动；保留 MySQL 驱动，可同时支持两种库，通过 profile 切换。
2. **配置**：新增 **`backend/src/main/resources/application-supabase.yml`**，用于连接 Supabase 的 PostgreSQL。

**使用步骤：**

1. 在 [Supabase](https://supabase.com) 创建项目，进入 **Project Settings → Database**。
2. 复制连接信息：
   - **Connection string** 选 “URI”，形如：`postgresql://postgres.[ref]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres`
   - 或使用 Host、Port(5432/6543)、Database(`postgres`)、User、Password 自己拼 URL。
3. 设置环境变量（或直接改 `application-supabase.yml` 中的默认占位）：
   - `SUPABASE_DB_URL`：JDBC 地址，例如  
     `jdbc:postgresql://db.xxxx.supabase.co:5432/postgres`  
     （注意 Supabase 的 Host 可能是 `db.xxx.supabase.co`，端口 5432 直连或 6543 连接池）
   - `SUPABASE_DB_USER`：一般为 `postgres`
   - `SUPABASE_DB_PASSWORD`：Database 页面中的密码
4. 使用 **supabase**  profile 启动：

```bash
cd backend
mvn spring-boot:run -Dspring-boot.run.profiles=supabase
```

或：

```bash
export SPRING_PROFILES_ACTIVE=supabase
mvn spring-boot:run
```

首次启动时 JPA 会按实体在 Supabase 的 `public` schema 下自动建表（`ddl-auto: update`）。**Redis 仍需要**（Session 存储），继续使用本地或云 Redis 即可。

若希望**完全改用 Supabase、不再使用 MySQL**，可删除 `pom.xml` 中的 `mysql-connector-j` 依赖，并把默认 `application.yml` 里的 `datasource` 改为 PostgreSQL 连接（或保留 profile 方式，默认用 supabase profile）。

---

## 二、前端（Vite + React）

### 2.1 环境要求

| 项目   | 要求        |
|--------|-------------|
| Node.js | 18.x 或以上（推荐 20 LTS） |
| npm   | 9+ 或 pnpm / yarn |

### 2.2 安装依赖

在项目根目录下进入前端目录并安装依赖：

```bash
cd frontend
npm install
```

若使用 pnpm：

```bash
cd frontend
pnpm install
```

### 2.3 环境变量（可选）

当前前端通过 Vite 的 `loadEnv` 可读取根目录下的 `.env` 文件，例如：

- 开发：`.env.development` 或 `.env`
- 生产：`.env.production`

若后续对接后端 API，可增加：

```env
# 后端 API 基址（示例）
VITE_API_BASE=http://localhost:8080/api
```

前端代码中通过 `import.meta.env.VITE_API_BASE` 使用。若使用 Vite 代理，见下节。

### 2.4 开发环境代理（可选）

开发时若希望请求同源、避免跨域，可在 **`frontend/vite.config.ts`** 中增加 proxy，把 `/api` 转到后端：

```ts
export default defineConfig(({ mode }) => {
  const env = loadEnv(mode, '.', '');
  return {
    server: {
      port: 3000,
      host: '0.0.0.0',
      proxy: {
        '/api': {
          target: 'http://localhost:8080',
          changeOrigin: true,
        },
      },
    },
    // ... 其余配置
  };
});
```

则前端请求 `/api/jobs` 等会由 Vite 转发到 `http://localhost:8080/api/jobs`。

### 2.5 启动方式

**开发模式（热更新）：**

```bash
cd frontend
npm run dev
```

或：

```bash
pnpm dev
```

默认访问：**http://localhost:3000**（若配置了 `host: '0.0.0.0'`，局域网也可通过本机 IP:3000 访问）。

**生产构建与预览：**

```bash
cd frontend
npm run build
npm run preview
```

`preview` 会启动一个本地静态服务器，用于查看打包后的效果。

---

## 三、联调：前端 + 后端同时运行

1. **启动 MySQL、Redis**（若尚未启动）。
2. **启动后端**：
   ```bash
   cd backend
   mvn spring-boot:run
   ```
   确认：浏览器或 curl 访问 `http://localhost:8080/api/jobs` 有正常 JSON 返回。
3. **启动前端**：
   ```bash
   cd frontend
   npm run dev
   ```
   浏览器打开 **http://localhost:3000**。

若前端已按上文配置了 `/api` 代理到 `http://localhost:8080`，则页面中请求 `/api/xxx` 会自动转发到后端；否则需在前端将接口基址配置为 `http://localhost:8080/api`。

---

## 四、常见问题

- **后端启动报错 “Access denied for user”**  
  检查 MySQL 用户名、密码、`DB_PASSWORD` 环境变量，以及该用户是否有 `campusrecruit` 库权限。

- **后端报错 “Unable to connect to Redis”**  
  确认 Redis 已启动且 `application.yml` 中 `host/port` 正确。

- **前端白屏或接口 404**  
  确认后端已启动、context-path 为 `/api`，且前端请求的 baseURL 为 `http://localhost:8080/api`（或通过 proxy 指向后端）。

- **表不存在**  
  项目使用 JPA `ddl-auto: update`，首次启动会自动建表；若需完全重建，可先删库再建空库后重启后端。

---

## 五、配置与端口汇总

| 说明           | 默认值 / 位置 |
|----------------|----------------|
| 后端端口       | 8080           |
| 后端上下文路径 | /api           |
| 后端接口基址   | http://localhost:8080/api |
| 前端开发端口   | 3000           |
| 前端开发地址   | http://localhost:3000     |
| MySQL 库名     | campusrecruit（可改）     |
| Redis         | localhost:6379（可改）    |
| 后端主配置     | backend/src/main/resources/application.yml |
| 测试配置       | backend/src/test/resources/application-test.yml |

---

## 六、前端 E2E 自动化测试（Playwright）

项目已接入 Playwright，使用 Chromium（Chrome DevTools 协议）对前端进行端到端自动化测试。

### 6.1 环境与依赖

- 在 **frontend** 目录已配置：`@playwright/test`、`playwright.config.ts`、`e2e/*.spec.ts`。
- 首次需安装浏览器：在 `frontend` 下执行 `npx playwright install chromium`。

### 6.2 运行前准备

1. **后端必须先启动**（E2E 会真实请求 `/api`）：
   ```bash
   cd backend
   mvn spring-boot:run
   ```
2. **前端无需手动启动**：Playwright 会根据 `playwright.config.ts` 的 `webServer` 自动执行 `npm run dev` 并等待 `http://localhost:3000` 就绪后再跑用例。

### 6.3 一键运行所有 E2E 测试

在 **frontend** 目录执行：

```bash
cd frontend
npm run test:e2e
```

或：

```bash
cd frontend
npx playwright test
```

将依次执行：

- **01-home.spec.ts**：首页加载、行业/类型/地点/截止筛选、今日新增开关。
- **02-auth-quota.spec.ts**：微信登录（stub prompt 自动填入）、多次查询触发免费次数用尽、付费弹层及跳转会员页。
- **03-pages.spec.ts**：进度统计页、会员中心页、套餐列表、导航返回首页、登录后职位状态切换。

### 6.4 可选参数

- 无头模式（CI 适用）：`CI=1 npm run test:e2e` 或 `npx playwright test --headed=false`。
- 只跑某个文件：`npx playwright test e2e/01-home.spec.ts`。
- 调试：`npx playwright test --debug`。

### 6.5 在 Cursor 中使用 Playwright MCP Server（浏览器自动化）

项目已配置 **Playwright MCP Server**（Microsoft 官方），便于在 Cursor 内通过 AI 控制浏览器（导航、点击、截图、可访问性快照等）。

- **配置文件**：`.cursor/mcp.json`，已添加 `playwright` 服务器，命令为 `npx -y @playwright/mcp@latest`。
- **使用前**：在项目或本机安装 Playwright 浏览器（若尚未安装）：
  ```bash
  cd frontend
  npx playwright install chromium
  ```
- **启用方式**：保存 `mcp.json` 后，重启 Cursor 或通过 **Ctrl+Shift+P** → **“MCP: Restart Servers”** 重新加载；在对话中即可使用 Playwright MCP 提供的工具（如 `browser_navigate`、`browser_snapshot`、`browser_click` 等）。

### 6.6 使用 Playwright MCP 做前端功能交互测试（手动/对话驱动）

在已安装 Chromium 且 Cursor 已加载 Playwright MCP 的前提下，可按下面流程用「对话 + 浏览器」对前端做功能交互测试。

#### 第一步：启动本项目的后端与前端

1. **启动后端**（提供 `/api` 接口）：
   ```bash
   cd backend
   mvn spring-boot:run
   ```
2. **启动前端**（Vite 开发服务器）：
   ```bash
   cd frontend
   npm run dev
   ```
   确认浏览器可访问 **http://localhost:3000**。

#### 第二步：在 Cursor 对话中驱动浏览器

在 Cursor 的 AI 对话里用自然语言描述你要做的操作，AI 会通过 Playwright MCP 控制浏览器执行。例如：

| 你说的话（示例） | AI 会做的操作 |
|------------------|----------------|
| 「打开 http://localhost:3000」 | 使用 `browser_navigate` 打开前端首页 |
| 「看一下当前页面结构」 | 使用 `browser_snapshot` 获取可访问性快照并解读 |
| 「点击微信登录」 | 使用 `browser_click` 点击「微信登录」按钮 |
| 「在 prompt 里输入 openid_test 并确定」 | 通过 MCP 或提示你：需在浏览器弹出的 prompt 中手动输入并确认（或由 AI 用 dialog 处理若支持） |
| 「选择行业为互联网，再点查询筛选」 | 先 `browser_select_option` 或 `browser_click` 选行业，再点击「查询筛选」 |
| 「打开会员中心页面」 | 点击导航「会员中心」或 `browser_navigate` 到 `#/vip` |
| 「截一张图」 | 使用 `browser_take_screenshot` 保存当前页面截图 |

**常用 MCP 能力**：`browser_navigate`、`browser_snapshot`、`browser_click`、`browser_type`、`browser_fill_form`、`browser_select_option`、`browser_take_screenshot` 等。

#### 第三步：建议的测试场景（可逐条在对话中让 AI 执行）

1. **首页**：打开首页 → 看「最新职位」与列表/空状态 → 改行业/类型/地点 → 点「查询筛选」→ 看结果或「暂无相关职位」。
2. **登录**：点「微信登录」→ 在弹窗输入 stub code（如 `openid_123`）→ 确认后看是否显示用户信息或「免费剩余」。
3. **免费次数**：登录后多次点击「查询筛选」，直到出现「今日免费次数已用完」弹层 → 点「立即开通会员」跳转会员页。
4. **会员页**：进入会员中心 → 看套餐列表是否加载 → 点击某套餐（会触发 confirm/下单逻辑）。
5. **进度页**：导航到进度统计 → 未登录时应提示「请登录」；登录后应显示「我的投递看板」与统计。
6. **职位状态**：在首页任选一职位卡片，将状态下拉框改为「已投递」等，确认请求成功且 UI 更新。

按上述顺序在对话中一句句说（如「先打开 localhost:3000」「再点微信登录」），即可用 Playwright MCP 完成前端功能交互测试；若某步失败，可根据 AI 返回的 snapshot 或截图排查。

以上为完整配置与启动说明，按需修改后即可本地运行与联调。
