# 在 Supabase 中创建项目所需表的完整步骤

本文说明如何在 Supabase 中手动创建本项目的全部表结构（与后端 JPA 实体一致），适合「不用 JPA 自动建表、先在手头建好表再连应用」的方式。

---

## 一、前置准备

1. 已注册 [Supabase](https://supabase.com) 并创建好一个项目。
2. 知道项目连接信息（后续后端连接 Supabase 时会用到）：
   - 在 Supabase 控制台 → **Project Settings** → **Database** 可看到 **Connection string**、Host、Port、Database、User、Password。

---

## 二、在 Supabase 中执行建表 SQL

### 步骤 1：打开 SQL Editor

1. 登录 [Supabase Dashboard](https://app.supabase.com)。
2. 选择你的项目。
3. 左侧菜单点击 **SQL Editor**（或 **Database** → **SQL Editor**）。

### 步骤 2：新建查询并粘贴脚本

1. 点击 **New query**（或 **+ New query**）。
2. 打开本项目中的建表脚本：
   - 文件路径：**`backend/supabase-schema.sql`**
3. 将脚本**全部复制**到 SQL Editor 的编辑框中。

### 步骤 3：执行脚本

1. 点击右下角 **Run**（或使用快捷键执行）。
2. 若没有报错，会提示执行成功；若有错误，根据提示修改（例如表已存在时可改为先删表再建，或脚本中已使用 `IF NOT EXISTS`，一般可重复执行）。

### 步骤 4：确认表已创建

1. 左侧进入 **Table Editor**（或 **Database** → **Tables**）。
2. 在 **public** schema 下应能看到以下 5 张表：

| 表名 | 说明 |
|------|------|
| **job** | 招聘职位 |
| **app_user** | 用户（微信 openid 等） |
| **user_job_status** | 用户对职位的投递状态 |
| **user_member** | 会员开通记录（方案 A） |
| **payment_order** | 支付订单 |

点击任一张表可查看列、索引、约束是否与脚本一致。

---

## 三、脚本说明（可选阅读）

- **backend/supabase-schema.sql** 中的表、列、索引、唯一约束均与后端 JPA 实体一一对应，便于应用使用 `ddl-auto: update` 时不再改表结构，或直接关闭自动 DDL 仅连接已有表。
- 主键为 `BIGINT GENERATED BY DEFAULT AS IDENTITY`，与 JPA `GenerationType.IDENTITY` 兼容。
- 未建外键（user_id、job_id 等仅为普通列），与当前实体定义一致；若以后要加外键，可在该脚本基础上追加 `ALTER TABLE ... ADD CONSTRAINT ...`。

---

## 四、建表后：用后端连接 Supabase

1. 在 Supabase 的 **Project Settings** → **Database** 中复制连接信息。
2. 配置后端（任选其一）：
   - 使用 **profile**：设置环境变量  
     `SUPABASE_DB_URL`、`SUPABASE_DB_USER`、`SUPABASE_DB_PASSWORD`，并设置  
     `SPRING_PROFILES_ACTIVE=supabase`，然后启动后端（参见《配置与启动说明.md》中的「1.8 使用 Supabase 作为数据库」）。
   - 或直接在 `application.yml`（或 `application-supabase.yml`）中填写 Supabase 的 JDBC URL、用户名、密码。
3. 建议首次连接时把 JPA 的 `ddl-auto` 设为 **`validate`** 或 **`none`**，避免自动改表；若希望仍由 Hibernate 维护表结构，可继续使用 **`update`**（通常不会破坏已有表）。

完成以上步骤后，Supabase 中即具备项目所需的全部表，后端可正常读写。
